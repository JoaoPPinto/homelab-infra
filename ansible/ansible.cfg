# config file for ansible -- http://ansible.com/
# ==============================================

[defaults]
inventory = ./inventory.yaml
roles_path = ./roles

remote_user = admin
gathering = explicit

nocows = 1

callbacks_enabled = profile_tasks

pipelining = True

